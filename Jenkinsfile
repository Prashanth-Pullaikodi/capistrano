pipeline {
    agent any

    stages {
        stage ('Build Stage') {

            steps {
              
                    sh 'ls -t'

            }
        }

        stage ('Push Stage') {

            steps {

                    sh 'ls -a '

            }
        }


        stage ('Deployment Stage') {
            steps {
 
                    sh 'ls  '

            }
        }
    }
}
